{
    // The deployment path: build target location if "deploy" is specified
    // On windows please use \\ for path names!
    "deployment_path": "##DEPLOYMENTPATH##",

    // The zip path: zip target location if "zip" is specified
    // On windows please use \\ for path names!
    //"zip_path": "##ZIPPATH##",

    // Doc files target location besides the default location in the build directory
    // On windows please use \\ for path names!
    //"doc_path": "##DOCPATH##",

    // Specify if Grace should try to minify your JavaScript files (default is false)
    "minify_js": false,

    // Specify if Grace should try to minify your CSS files (default is false)
    "minify_css": false,

    // The following is a collection of URLs used by the upload command. If the 'login' URL is
    // not specified, the upload URL is used for login purposes.
    "urls": {
        //"upload": ""
    },

    // Credentials for login purposes used by the upload command.
    "credentials": {
        "username": "",
        "password": ""
    }
}
